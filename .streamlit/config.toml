# Streamlit 화면 테마 설정
[theme]
primaryColor = "#E03131"          # 강조색(빨강 계열 - 사고/위험 느낌)
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F1F3F5"
textColor = "#212529"
font = "sans serif"

[server]
# 업로드/요청 관련 기본값. 배포 시 그대로 사용 가능
headless = true
